package classes.TripeEx

fun main(){

    var tripe = Tripe(false, 40, 100, 50)

    tripe.usar()
    tripe.definirAltura(40)
    tripe.prontoParaGuardar()
    tripe.dobrar()
    tripe.guardar()
    tripe.desdobrar()
    tripe.usar()
    tripe.definirAltura(60)
    tripe.usar()
}